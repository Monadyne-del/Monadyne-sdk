{
  "name": "solana-monad-sdk",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo implementing Monad core concepts adapted to Solana",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -w -r run build",
    "test": "echo \"No tests configured\""
  },
  "devDependencies": {
    "typescript": "^5.1.6",
    "ts-node": "^10.9.1",
    "pnpm": "^8.0.0",
    "@types/node": "^20.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
