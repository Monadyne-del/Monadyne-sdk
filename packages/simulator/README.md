# @sdk/simulator

Provides a deterministic simulator that builds a DAG from instructions, layers them for parallel execution, and produces a compute/latency simulation report.

Build:

```bash
pnpm -F @sdk/simulator build
```
